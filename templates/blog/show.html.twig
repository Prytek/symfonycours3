{% extends 'base.html.twig' %}

{% block title %}Blog{% endblock %}

{% block body %}
            <h1 class="text-center my-5">{{ blog.title }}</h1>
            <div class="container-fluid">
                <div class="col">
                    <p class="text-center">{{ blog.content }}</p>
                    <p class="ml-4"><em>Publier le: </em>{{ blog.createAt ? blog.createAt|date('d-m-Y H:i') : '' }}</p>
                    {% if blog.modifyAt != null %}
                    <p class="ml-4"><em>Modifier le: </em>{{ blog.modifyAt ? blog.modifyAt|date('d-m-Y H:i') : '' }}</p>
                    {% endif %}
                </div>
                <div class="text-center">
                    <a href="{{ path('app_blog_index') }}">Retour à la liste</a>
                </div> 
            </div>
     {#<a href="{{ path('app_blog_edit', {'id': blog.id}) }}">edit</a> #}       
    {# {{ include('blog/_delete_form.html.twig') }} #}
{% endblock %}
